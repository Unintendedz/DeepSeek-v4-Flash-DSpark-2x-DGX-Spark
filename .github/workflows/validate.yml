name: Validate

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  cpu-recipe-gates:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate recipe and runtime patches
        run: bash scripts/ci-validate.sh
